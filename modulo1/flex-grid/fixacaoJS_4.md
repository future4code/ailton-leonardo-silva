    <!-- const pegaRepetido = arrayDeNumeros.filter(num => num === numeroEscolhido).length
        if  (pegaRepetido === 0) {
            return `Número não encontrado`
        } else {
            return `O número ${numeroEscolhido} aparece ${quantidade}x`
        };  -->